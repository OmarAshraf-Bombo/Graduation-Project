@model IEnumerable<FinalWebSite.Models.Mostanadat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mostanad_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Msale7.Msl7a_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mostanad_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Msale7.Msl7a_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Mostanad_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Mostanad_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Mostanad_ID })
        </td>
    </tr>
}

</table>
