@model IEnumerable<FinalWebSite.Models.Msale7>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Msl7a_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Msl7a_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Msl7a_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Msl7a_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Msl7a_ID })
        </td>
    </tr>
}

</table>
